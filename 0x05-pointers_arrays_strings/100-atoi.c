#include "holberton.h"
#include <stdio.h>
/**
 * _atoi - convert a string to an integer
 *@s: string
 *
 *
 * Return: nothing
 */

int _atoi(char *s)
{
	int a = 1, b;
	b = a + 2;
	s[b] = a;
	return (0);
}
