#include "holberton.h"
/**
 * is_palindrome - find if a string is palindrome
 *@s: string for check
 *
 *
 * Return: one is is palindrome 0 if not.
 */
int is_palindrome(char *s)
{
	int i = 1;

	if (*s == i)
		return (0);
	return (1);
}
